const translations = {
  "zh-CN": {
    metaTitle: "WA Chem — 化学结构工作台",
    metaDescription: "WA Chem 官方门户：原生 Mac 与 iPad 化学结构工作台，支持数据自主存储、iCloud 同步及 WA-DD 互联。",
    skipLink: "跳到主要内容",
    brandHome: "WA Chem 首页",
    mainNavigation: "主要导航",
    navProduct: "产品",
    navReleases: "发布",
    navDocs: "文档",
    heroTitle: "把化学结构，<br />自然地画出来",
    heroBody: '从手绘与照片识别，到结构校正、保存和导出。<br />WA Chem 正在构建一套更顺手的化学结构工作台。<br />可接入 <a href="https://wa-dd.fuluwa.top">WA-DD</a> 进行配体结构编辑管理。',
    viewReleases: "下载安装",
    viewProject: "查看项目",
    releasePending: "首个公开版本准备中",
    heroAlt: "WA Chem 中由 AI 辅助识别与编辑化学结构的理念图",
    productFlow: "WA Chem 核心流程",
    nativeExperience: "Mac 与 iPad 原生统一体验",
    deviceSwitcher: "切换设备界面",
    showMac: "显示 Mac 界面",
    showIpad: "显示 iPad 界面",
    pauseCarousel: "暂停自动轮播",
    playCarousel: "继续自动轮播",
    nativeRendering: "原生 SwiftUI + Metal",
    macCaption: "Mac 原生界面 · ACS 1996 文档样式",
    touchAndPencil: "触控 + Apple Pencil",
    icloudSync: "iCloud 同步",
    naturalDrawing: "自然绘制",
    naturalDrawingBody: "围绕原子、键、环和选择建立低摩擦的编辑体验。",
    recognitionCorrection: "识别与校正",
    recognitionCorrectionBody: "让截图、论文图片和手绘结构重新成为可编辑内容。",
    saveExchange: "保存与交换",
    saveExchangeBody: "在同一工作区完成保存、格式导入导出与后续计算。",
    interfaceTitle: "专注真实的绘制体验",
    interfaceBody: "熟悉的元素、键型和结构属性集中在同一工作区。Mac 原生界面已支持 ACS 1996 文档样式。",
    interfaceAlt: "WA Chem Mac 原生编辑器界面，画布使用 ACS 1996 文档样式",
    advantageTitle: "围绕你的数据，做一套真正原生的工作台",
    advantageBody: "从设备内的数据归属，到跨设备协作与资产组织，WA Chem 的产品逻辑为长期研究工作而设计。",
    dataOwnership: "数据自主",
    dataOwnershipBody: "结构与项目数据由你自行存储，并可通过 iCloud 在 Mac 与 iPad 间同步。",
    nativeUnified: "原生统一",
    nativeUnifiedBody: "同一套 iPad 与 Mac 应用，纯 Apple 原生开发，不包含 JavaScript / TypeScript Web 运行层。",
    waDdConnection: "WA-DD 互联",
    waDdConnectionBody: '连接 <a href="https://wa-dd.fuluwa.top">WA-DD</a>，在工作台中选择、编辑并管理配体结构。',
    assetManagement: "资产管理",
    assetManagementBody: "围绕项目、资产与分子的关系深度优化，让查找、追加、保存与导出更清晰。",
    platformTitle: "Mac 与 iPad，统一的不只是界面",
    platformBody: "同一套原生 Apple 应用，共享编辑器、文档和资产管理逻辑；针对键鼠、触控与 Apple Pencil 自然适配。",
    computePaths: "不同版本的模型算力来源",
    localCompute: "使用本地算力",
    localComputeBody: "模型在 Apple 设备本地运行，延续原生应用的数据与工作流体验。",
    serverCompute: "使用服务器算力",
    serverComputeBody: "模型由部署 WA Chem 的服务器运行，浏览器负责连接与操作。",
    ipadAlt: "WA Chem 在 iPad 上运行的原生编辑器界面",
    ipadCaption: "iPad 原生界面 · 与 Mac 共享产品逻辑",
    getWaChem: "获取 WA Chem",
    releaseBody: "发布包将通过 GitHub Releases 提供。每个版本都包含变更说明、校验信息和对应平台的下载文件。",
    goToReleases: "前往 Releases",
    viewOnGitHub: "在 GitHub 查看 wachem-web",
    currentStatus: "当前状态",
    chooseDownload: "选择下载包",
    viewReleaseNotes: "查看版本说明",
    webDeployPackage: "独立 Web 部署包",
    webDeployPackageMeta: "服务器部署 · tar.gz",
    vosPackage: "VOS App 包",
    vosPackageMeta: "VOS 应用安装 · pull.tar",
    macPackageMeta: "Apple Silicon · dmg",
    waitingRelease: "等待发布",
    appStore: "应用商店",
    appStoreMeta: "macOS 与 iPadOS · 即将上架",
    appStoresPending: "Mac App Store 与 iPad App Store 尚未上架",
    comingSoon: "即将上架",
    checksumsPending: "SHA256 校验文件将在版本发布后提供",
    checksumsAvailable: "下载 SHA256SUMS 校验文件",
    gettingStartedSteps: "开始使用的三个步骤",
    download: "下载",
    install: "安装",
    startDrawing: "开始绘制",
    backToTop: "返回顶部",
    footerNavigation: "页脚导航",
    latestVersion: (label) => `最新版本 ${label}`,
    downloadLabel: (title, filename) => `下载 ${title}：${filename}`,
    languageAction: "Switch to English",
    languageLabel: "EN",
  },
  en: {
    metaTitle: "WA Chem — Chemical Structure Workspace",
    metaDescription: "The official WA Chem portal: a native chemical structure workspace for Mac and iPad with user-owned storage, iCloud sync, and WA-DD connectivity.",
    skipLink: "Skip to main content",
    brandHome: "WA Chem home",
    mainNavigation: "Main navigation",
    navProduct: "Product",
    navReleases: "Releases",
    navDocs: "Docs",
    heroTitle: "Draw chemical structures,<br />naturally",
    heroBody: 'Turn sketches and images into editable structures, then refine, save, and export them. <br />WA Chem is building a smoother workspace for chemical structure work. <br />Connect to <a href="https://wa-dd.fuluwa.top">WA-DD</a> to edit and manage ligand structures.',
    viewReleases: "Download & install",
    viewProject: "View project",
    releasePending: "First public release in preparation",
    heroAlt: "Concept artwork showing AI-assisted chemical structure recognition and editing in WA Chem",
    productFlow: "WA Chem core workflow",
    nativeExperience: "Unified native experience on Mac and iPad",
    deviceSwitcher: "Switch device interface",
    showMac: "Show the Mac interface",
    showIpad: "Show the iPad interface",
    pauseCarousel: "Pause automatic carousel",
    playCarousel: "Resume automatic carousel",
    nativeRendering: "Native SwiftUI + Metal",
    macCaption: "Native Mac interface · ACS 1996 document style",
    touchAndPencil: "Touch + Apple Pencil",
    icloudSync: "iCloud sync",
    naturalDrawing: "Natural drawing",
    naturalDrawingBody: "A low-friction editing experience built around atoms, bonds, rings, and selection.",
    recognitionCorrection: "Recognize and refine",
    recognitionCorrectionBody: "Turn screenshots, figures, and hand-drawn structures back into editable content.",
    saveExchange: "Save and exchange",
    saveExchangeBody: "Save, import, export, and continue downstream work from one workspace.",
    interfaceTitle: "A focused drawing experience",
    interfaceBody: "Elements, bond types, and structure properties stay together in one workspace. The native Mac interface supports the ACS 1996 document style.",
    interfaceAlt: "Native WA Chem editor on Mac with an ACS 1996 styled canvas",
    advantageTitle: "A truly native workspace built around your data",
    advantageBody: "From data ownership on your devices to cross-device work and asset organization, WA Chem is designed for long-term research workflows.",
    dataOwnership: "Your data, your storage",
    dataOwnershipBody: "You control where structures and project data are stored, with optional iCloud sync between Mac and iPad.",
    nativeUnified: "One native app",
    nativeUnifiedBody: "A unified iPad and Mac app built with native Apple technologies, with no JavaScript or TypeScript web runtime.",
    waDdConnection: "WA-DD connectivity",
    waDdConnectionBody: 'Connect to <a href="https://wa-dd.fuluwa.top">WA-DD</a> to select, edit, and manage ligand structures from the workspace.',
    assetManagement: "Asset management",
    assetManagementBody: "Deeply optimized relationships between projects, assets, and molecules make finding, adding, saving, and exporting clearer.",
    platformTitle: "Mac and iPad, unified beyond the interface",
    platformBody: "One native Apple app shares the same editor, documents, and asset model while adapting naturally to keyboard, pointer, touch, and Apple Pencil.",
    computePaths: "Model compute paths by product",
    localCompute: "On-device compute",
    localComputeBody: "Models run locally on Apple devices, preserving the native app's data and workflow experience.",
    serverCompute: "Server compute",
    serverComputeBody: "Models run on the server where WA Chem is deployed, while the browser provides access and interaction.",
    ipadAlt: "The native WA Chem editor running on iPad",
    ipadCaption: "Native iPad interface · Shared product logic with Mac",
    getWaChem: "Get WA Chem",
    releaseBody: "Packages are distributed through GitHub Releases. Each version includes release notes, checksums, and platform-specific downloads.",
    goToReleases: "Go to Releases",
    viewOnGitHub: "View wachem-web on GitHub",
    currentStatus: "Current status",
    chooseDownload: "Choose a package",
    viewReleaseNotes: "View release notes",
    webDeployPackage: "Standalone Web package",
    webDeployPackageMeta: "Server deployment · tar.gz",
    vosPackage: "VOS App package",
    vosPackageMeta: "VOS app installation · pull.tar",
    macPackageMeta: "Apple Silicon · dmg",
    waitingRelease: "Awaiting release",
    appStore: "App Store",
    appStoreMeta: "macOS and iPadOS · Coming soon",
    appStoresPending: "Mac App Store and iPad App Store versions are not yet available",
    comingSoon: "Coming soon",
    checksumsPending: "SHA256 checksums will be available with the release",
    checksumsAvailable: "Download SHA256SUMS",
    gettingStartedSteps: "Three steps to get started",
    download: "Download",
    install: "Install",
    startDrawing: "Start drawing",
    backToTop: "Back to top",
    footerNavigation: "Footer navigation",
    latestVersion: (label) => `Latest ${label}`,
    downloadLabel: (title, filename) => `Download ${title}: ${filename}`,
    languageAction: "切换到中文",
    languageLabel: "中",
  },
};

const releaseStatus = document.querySelectorAll("[data-release-status]");
const releasePageLinks = document.querySelectorAll("[data-release-page]");
const languageToggle = document.querySelector("[data-language-toggle]");
const languageLabel = document.querySelector("[data-language-label]");
const experienceSection = document.querySelector(".experience-section");
const experienceTabs = [...document.querySelectorAll("[data-experience-tab]")];
const experiencePanels = [...document.querySelectorAll("[data-experience-panel]")];
const experienceCounter = document.querySelector("[data-experience-counter]");
const experienceAutoplay = document.querySelector("[data-experience-autoplay]");
const reduceMotionQuery = window.matchMedia("(prefers-reduced-motion: reduce)");
const experienceAutoplayDelay = 7000;
const downloadMatchers = {
  deploy: /^wa-chem_deploy_[^/]+\.tar\.gz$/,
  vos: /^wa-chem_[^/]+_pull\.tar$/,
  mac: /^wa-chem-mac_[^/]+_aarch64\.dmg$/,
  checksums: /^SHA256SUMS$/,
};

let activeLanguage = "zh-CN";
let latestRelease = null;
let experienceAutoplayTimer = null;
let experienceInView = false;
let experienceTemporarilyPaused = false;
let experienceUserPaused = false;

const requestedLanguage = new URLSearchParams(window.location.search).get("lang");
if (requestedLanguage === "en" || requestedLanguage === "zh-CN") {
  activeLanguage = requestedLanguage;
} else {
  try {
    if (localStorage.getItem("wa-chem-language") === "en") activeLanguage = "en";
  } catch {
    // Storage can be unavailable in privacy-restricted browser contexts.
  }
}

function text(key) {
  return translations[activeLanguage][key];
}

function formatBytes(bytes) {
  if (!Number.isFinite(bytes) || bytes <= 0) return "";
  const units = ["B", "KB", "MB", "GB"];
  const unit = Math.min(Math.floor(Math.log(bytes) / Math.log(1024)), units.length - 1);
  const value = bytes / 1024 ** unit;
  return `${value >= 10 || unit === 0 ? value.toFixed(0) : value.toFixed(1)} ${units[unit]}`;
}

function connectDownload(key, asset) {
  const link = document.querySelector(`[data-download-key="${key}"]`);
  if (!link || !asset) return;
  link.href = asset.browser_download_url;
  link.classList.remove("is-pending");
  const title = link.querySelector("strong")?.textContent || text("download");
  link.setAttribute("aria-label", text("downloadLabel")(title, asset.name));
  const meta = link.querySelector("[data-download-meta]");
  if (meta) meta.textContent = `${asset.name} · ${formatBytes(asset.size)}`;
  if (key === "checksums") link.textContent = text("checksumsAvailable");
}

function applyRelease(release) {
  if (!release) return;
  const label = release.name || release.tag_name;
  if (!label) return;
  releaseStatus.forEach((element) => {
    element.textContent = text("latestVersion")(label);
  });
  releasePageLinks.forEach((link) => {
    link.href = release.html_url;
  });
  Object.entries(downloadMatchers).forEach(([key, matcher]) => {
    connectDownload(key, release.assets.find((asset) => matcher.test(asset.name)));
  });
}

function applyLanguage(language) {
  activeLanguage = language;
  document.documentElement.lang = language;
  document.title = text("metaTitle");
  document.querySelectorAll("[data-i18n]").forEach((element) => {
    element.textContent = text(element.dataset.i18n);
  });
  document.querySelectorAll("[data-i18n-html]").forEach((element) => {
    element.innerHTML = text(element.dataset.i18nHtml);
  });
  document.querySelectorAll("[data-i18n-aria]").forEach((element) => {
    element.setAttribute("aria-label", text(element.dataset.i18nAria));
  });
  document.querySelectorAll("[data-i18n-title]").forEach((element) => {
    element.title = text(element.dataset.i18nTitle);
  });
  document.querySelectorAll("[data-i18n-alt]").forEach((element) => {
    element.alt = text(element.dataset.i18nAlt);
  });
  document.querySelectorAll("[data-i18n-content]").forEach((element) => {
    element.setAttribute("content", text(element.dataset.i18nContent));
  });
  languageToggle.setAttribute("aria-label", text("languageAction"));
  languageToggle.title = text("languageAction");
  languageLabel.textContent = text("languageLabel");
  applyRelease(latestRelease);
}

function selectExperience(device, moveFocus = false) {
  if (device !== "mac" && device !== "ipad") return;
  experienceSection.dataset.device = device;
  experienceTabs.forEach((tab) => {
    const isActive = tab.dataset.experienceTab === device;
    tab.setAttribute("aria-selected", String(isActive));
    tab.tabIndex = isActive ? 0 : -1;
    if (isActive && moveFocus) tab.focus();
  });
  experiencePanels.forEach((panel) => {
    const isActive = panel.dataset.experiencePanel === device;
    panel.classList.toggle("is-active", isActive);
    panel.setAttribute("aria-hidden", String(!isActive));
    panel.inert = !isActive;
  });
  experienceCounter.textContent = device === "mac" ? "01" : "02";
}

function stopExperienceAutoplay() {
  window.clearTimeout(experienceAutoplayTimer);
  experienceAutoplayTimer = null;
}

function scheduleExperienceAutoplay() {
  stopExperienceAutoplay();
  if (!experienceInView || experienceTemporarilyPaused || experienceUserPaused || reduceMotionQuery.matches || document.hidden) return;
  experienceAutoplayTimer = window.setTimeout(() => {
    const nextDevice = experienceSection.dataset.device === "mac" ? "ipad" : "mac";
    selectExperience(nextDevice);
    scheduleExperienceAutoplay();
  }, experienceAutoplayDelay);
}

function setExperienceUserPaused(isPaused) {
  experienceUserPaused = isPaused;
  experienceAutoplay.dataset.paused = String(isPaused);
  const translationKey = isPaused ? "playCarousel" : "pauseCarousel";
  experienceAutoplay.dataset.i18nAria = translationKey;
  experienceAutoplay.dataset.i18nTitle = translationKey;
  experienceAutoplay.setAttribute("aria-label", text(translationKey));
  experienceAutoplay.title = text(translationKey);
  experienceAutoplay.setAttribute("aria-pressed", String(isPaused));
  scheduleExperienceAutoplay();
}

experienceTabs.forEach((tab) => {
  tab.addEventListener("click", () => {
    selectExperience(tab.dataset.experienceTab);
    scheduleExperienceAutoplay();
  });
  tab.addEventListener("keydown", (event) => {
    const navigationKeys = ["ArrowRight", "ArrowLeft", "Home", "End"];
    if (!navigationKeys.includes(event.key)) return;
    const currentIndex = experienceTabs.indexOf(tab);
    let nextIndex = currentIndex;
    if (event.key === "ArrowRight") nextIndex = (currentIndex + 1) % experienceTabs.length;
    if (event.key === "ArrowLeft") nextIndex = (currentIndex - 1 + experienceTabs.length) % experienceTabs.length;
    if (event.key === "Home") nextIndex = 0;
    if (event.key === "End") nextIndex = experienceTabs.length - 1;
    event.preventDefault();
    selectExperience(experienceTabs[nextIndex].dataset.experienceTab, true);
    scheduleExperienceAutoplay();
  });
});

experienceAutoplay.addEventListener("click", () => setExperienceUserPaused(!experienceUserPaused));

experienceSection.addEventListener("mouseenter", () => {
  experienceTemporarilyPaused = true;
  stopExperienceAutoplay();
});

experienceSection.addEventListener("mouseleave", () => {
  experienceTemporarilyPaused = false;
  scheduleExperienceAutoplay();
});

experienceSection.addEventListener("focusin", () => {
  experienceTemporarilyPaused = true;
  stopExperienceAutoplay();
});

experienceSection.addEventListener("focusout", (event) => {
  if (experienceSection.contains(event.relatedTarget)) return;
  experienceTemporarilyPaused = false;
  scheduleExperienceAutoplay();
});

document.addEventListener("visibilitychange", scheduleExperienceAutoplay);
reduceMotionQuery.addEventListener("change", scheduleExperienceAutoplay);

const experienceObserver = new IntersectionObserver(([entry]) => {
  experienceInView = entry.isIntersecting;
  scheduleExperienceAutoplay();
}, { threshold: 0.4 });
experienceObserver.observe(experienceSection);

languageToggle.addEventListener("click", () => {
  const language = activeLanguage === "zh-CN" ? "en" : "zh-CN";
  const url = new URL(window.location.href);
  url.searchParams.set("lang", language);
  history.replaceState(null, "", url);
  try {
    localStorage.setItem("wa-chem-language", language);
  } catch {
    // The current page can still switch languages without persistence.
  }
  applyLanguage(language);
});

applyLanguage(activeLanguage);
selectExperience("mac");
setExperienceUserPaused(false);

fetch("https://api.github.com/repos/huluxiaohuowa/wachem-web/releases/latest", {
  headers: { Accept: "application/vnd.github+json" },
})
  .then((response) => {
    if (!response.ok) throw new Error(`GitHub API returned ${response.status}`);
    return response.json();
  })
  .then((release) => {
    latestRelease = release;
    applyRelease(release);
  })
  .catch(() => {
    // Keep the localized static pre-release status when no public release exists yet.
  });
